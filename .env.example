# Replicache license key
VITE_REPLICACHE_LICENSE_KEY=

# Database Configuration

# Choose the database type: 'pgmem' (in-memory), 'postgres' (local instance), or 'supabase'
DB_TYPE=pgmem

# Database URL (required for 'postgres' and 'supabase' types)
# Uncomment and set the appropriate URL based on your DB_TYPE

# For local PostgreSQL instance:
# DATABASE_URL=postgresql://username:password@localhost:5432/postgres

# For Supabase:
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Note: Keep the DB_TYPE as 'pgmem' if you want to use the in-memory PostgreSQL.
# No DATABASE_URL is required for 'pgmem'.
